name: PR

on:
  pull_request:
    branches: main

jobs:
  e2e-test:
    uses: jill64/workflows/.github/workflows/run-playwright.yml@main
    with:
      test-command: export PUBLIC_SENTRY_DSN=${{ vars.PUBLIC_SENTRY_DSN }} && npm run test:e2e
